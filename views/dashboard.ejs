<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>My dashboard</h1>
    <p style="float:right; margin-right:90px;">Hello, <%= name  %> </p>

    <h3>My Blogs</h3>

    <% if(blogs.length > 0) { %> 
        <% blogs.forEach((blog) => { %>

    <a href="myblog/<%= blog.id %>">
      <span style="display:block;"><%= blog.title %> </span>
      <% let part = blog.content.slice(0, 25) %> 
      <span style="color:black; font-size:smaller; margin-bottom:30px;">  <%= part  %>   .....</span><br>
    </a><br>

    <% }) %> 
    <% }else{ %>
    <h3>No records found</h3>
    <% } %>

    <a href="/create-blog">Create new Blog</a><br><br>
    <a href="/logout">logout</a>
</body>
</html>